# 糖尿病智能服务管理系统 - 前端

基于 Vue 3 + Vite + Element Plus 构建的现代化糖尿病健康管理平台前端应用。

## ✨ 特性

- 🚀 **现代技术栈**: Vue 3 + Vite + Pinia + Vue Router
- 🎨 **优雅UI**: Element Plus 组件库，响应式设计
- 🔐 **完整认证**: JWT 登录、注册、权限控制
- 📱 **移动友好**: 支持各种设备尺寸
- 🎯 **TypeScript**: 可选的类型安全支持
- 📦 **自动化**: ESLint + Prettier 代码规范

## 🛠️ 技术栈

- **框架**: Vue 3.4+
- **构建工具**: Vite 5.0+
- **状态管理**: Pinia 2.1+
- **路由**: Vue Router 4.2+
- **UI组件**: Element Plus 2.4+
- **HTTP客户端**: Axios 1.6+
- **样式**: CSS3 + Element Plus主题
- **图标**: Element Plus Icons
- **日期处理**: Day.js
- **Cookie管理**: js-cookie

## 📦 安装

### 环境要求

- Node.js 16.0+ 或 18.0+
- npm 7.0+ 或 yarn 1.22+ 或 pnpm 7.0+

### 安装依赖

```bash
# 使用 npm
npm install

# 使用 yarn
yarn install

# 使用 pnpm
pnpm install
```

## 🚀 开发

### 启动开发服务器

```bash
# 开发模式启动
npm run dev

# 或者
yarn dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

## 📁 项目结构

```
fronted/
├── public/                 # 静态资源
├── src/                   # 源代码
│   ├── api/              # API接口
│   │   ├── auth.js       # 认证相关API
│   │   ├── user.js       # 用户相关API
│   │   └── request.js    # Axios配置
│   ├── components/       # 公共组件
│   ├── router/          # 路由配置
│   │   └── index.js     # 路由定义
│   ├── stores/          # Pinia状态管理
│   │   └── user.js      # 用户状态
│   ├── styles/          # 全局样式
│   │   └── index.css    # 全局CSS
│   ├── views/           # 页面组件
│   │   ├── auth/        # 认证页面
│   │   │   ├── Login.vue    # 登录
│   │   │   └── Register.vue # 注册
│   │   ├── user/        # 用户管理
│   │   │   └── Profile.vue  # 个人资料
│   │   ├── health/      # 健康数据
│   │   │   └── Records.vue  # 健康记录
│   │   ├── error/       # 错误页面
│   │   │   └── NotFound.vue # 404页面
│   │   └── Dashboard.vue    # 仪表板
│   ├── App.vue          # 根组件
│   └── main.js          # 应用入口
├── index.html           # HTML模板
├── package.json         # 项目配置
├── vite.config.js       # Vite配置
└── README.md           # 项目说明
```

## 🔧 配置

### API代理配置

开发环境下，Vite会将 `/api` 开头的请求代理到 `http://localhost:8080`，对应后端服务。

### 环境变量

支持的环境变量：

```bash
# API基础地址
VITE_API_BASE_URL=/api

# 应用标题
VITE_APP_TITLE=糖尿病智能服务管理系统

# 应用版本
VITE_APP_VERSION=1.0.0
```

## 🔐 认证流程

1. **登录**: 用户输入用户名/邮箱和密码
2. **令牌**: 后端返回访问令牌和刷新令牌
3. **存储**: 令牌存储在Cookie和localStorage中
4. **拦截**: Axios自动在请求头中添加令牌
5. **刷新**: 令牌过期时自动刷新
6. **登出**: 清除本地令牌信息

## 📱 主要功能

### 用户认证
- ✅ 用户登录
- ✅ 用户注册（邮箱验证）
- ✅ 密码修改
- ✅ 自动登录/记住密码

### 个人管理
- ✅ 个人资料修改
- ✅ 头像上传
- ✅ 账户安全设置

### 健康数据
- ✅ 血糖记录管理
- ✅ 血压记录管理
- ✅ 用药记录管理
- ✅ 数据可视化展示

### 系统功能
- ✅ 响应式布局
- ✅ 多语言支持准备
- ✅ 主题切换准备
- ✅ 权限控制

## 🔨 开发指南

### 代码规范

项目使用 ESLint 和 Prettier 进行代码规范化：

```bash
# 检查代码规范
npm run lint

# 自动修复
npm run lint --fix

# 格式化代码
npm run format
```

### 组件开发

推荐使用 Vue 3 Composition API：

```vue
<template>
  <div class="my-component">
    <!-- 模板内容 -->
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'

// 响应式数据
const loading = ref(false)
const form = reactive({
  name: '',
  email: ''
})
</script>

<style scoped>
.my-component {
  /* 组件样式 */
}
</style>
```

### API调用

使用封装的API方法：

```javascript
import { authAPI } from '@/api/auth'

// 登录
const result = await authAPI.login({ username, password })

// 获取用户信息
const userInfo = await authAPI.getCurrentUser()
```

## 🐛 常见问题

### 开发环境跨域问题
已通过 Vite 代理配置解决，确保后端服务运行在 8080 端口。

### 路由404问题
确保后端支持 History 模式，或考虑使用 Hash 模式。

### Element Plus样式问题
已配置自动导入，如遇到样式问题请检查 vite.config.js 配置。

## 🤝 贡献

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- [Vue.js](https://vuejs.org/) - 渐进式 JavaScript 框架
- [Element Plus](https://element-plus.org/) - Vue 3 组件库
- [Vite](https://vitejs.dev/) - 下一代前端构建工具
- [Pinia](https://pinia.vuejs.org/) - Vue 3 状态管理库 