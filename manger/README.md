# 糖尿病智能服务管理系统 - 管理端

## 项目简介

这是糖尿病智能服务管理系统的管理端，基于 Vue 3 + Vite + Element Plus 构建，为系统管理员提供用户管理、医生管理、咨询管理、健康数据管理等功能。

## 技术栈

- **前端框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **UI框架**: Element Plus
- **状态管理**: Pinia
- **路由管理**: Vue Router 4
- **HTTP客户端**: Axios
- **样式预处理**: CSS
- **开发语言**: JavaScript/TypeScript

## 项目结构

```
manger/
├── public/                 # 静态资源
├── src/
│   ├── api/               # API接口
│   │   ├── auth.js        # 认证API
│   │   ├── permission.js  # 权限API
│   │   └── request.js     # 请求配置
│   ├── components/        # 公共组件
│   │   └── layout/        # 布局组件
│   ├── directives/        # 自定义指令
│   │   └── permission.js  # 权限指令
│   ├── router/            # 路由配置
│   │   └── index.js
│   ├── stores/            # 状态管理
│   │   ├── user.js        # 用户状态
│   │   └── permission.js  # 权限状态
│   ├── styles/            # 样式文件
│   │   └── index.css      # 全局样式
│   ├── views/             # 页面组件
│   │   ├── admin/         # 管理员相关
│   │   ├── auth/          # 认证页面
│   │   ├── consultations/ # 咨询管理
│   │   ├── doctors/       # 医生管理
│   │   ├── health/        # 健康数据管理
│   │   ├── system/        # 系统设置
│   │   └── users/         # 用户管理
│   ├── App.vue            # 根组件
│   └── main.js            # 入口文件
├── index.html             # HTML模板
├── package.json           # 依赖配置
├── vite.config.js         # Vite配置
└── README.md             # 项目说明
```

## 功能特性

### 🔐 权限管理
- 基于角色的权限控制 (RBAC)
- 菜单级权限控制
- 功能按钮权限控制
- 动态菜单生成

### 👥 用户管理
- 用户列表管理
- 用户状态管理
- 用户角色分配
- 用户信息编辑

### 👨‍⚕️ 医生管理
- 医生信息管理
- 医生状态监控
- 医生权限配置
- 执业资质验证

### 💬 咨询管理
- 咨询记录查看
- 咨询状态管理
- 咨询质量监控
- 统计分析

### 📊 健康数据管理
- 用户健康数据统计
- 数据趋势分析
- 异常数据预警
- 报表生成

### ⚙️ 系统设置
- 系统参数配置
- 公告管理
- 日志管理
- 数据备份

## 开发环境

### 环境要求
- Node.js >= 16.0.0
- npm >= 7.0.0 或 yarn >= 1.22.0

### 安装依赖
```bash
# 使用 npm
npm install

# 或使用 yarn
yarn install
```

### 开发运行
```bash
# 启动开发服务器 (端口: 3001)
npm run dev

# 或
yarn dev
```

### 构建部署
```bash
# 构建生产版本
npm run build

# 或
yarn build
```

### 代码规范
```bash
# 代码格式检查
npm run lint

# 代码格式化
npm run format
```

## 配置说明

### 环境变量
创建 `.env.local` 文件设置环境变量:

```env
# API接口地址
VITE_API_BASE_URL=http://localhost:8080/api

# 应用标题
VITE_APP_TITLE=糖尿病智能服务管理系统管理端
```

### 开发配置
- **开发端口**: 3001 (避免与用户端冲突)
- **API代理**: `/api` -> `http://localhost:8080`
- **管理端标识**: 请求头自动添加 `Client-Type: admin`

## 权限系统

### 用户类型
- `userType: 2` - 系统管理员

### 权限验证
- 路由级权限验证
- 组件级权限控制
- 接口级权限认证

### 权限指令使用
```vue
<!-- 单个权限 -->
<el-button v-permission="'user:edit'">编辑用户</el-button>

<!-- 多个权限(任一) -->
<el-button v-permission="['user:edit', 'user:delete']">操作</el-button>

<!-- 角色权限 -->
<el-button v-role="'ADMIN'">管理员操作</el-button>
```

## API接口规范

### 请求格式
```javascript
// GET请求
api.get('/v1/admin/users', { page: 1, size: 10 })

// POST请求
api.post('/v1/admin/users', userData)

// PUT请求
api.put('/v1/admin/users/1', userData)

// DELETE请求
api.delete('/v1/admin/users/1')
```

### 响应格式
```json
{
  "code": 200,
  "success": true,
  "message": "操作成功",
  "data": {},
  "timestamp": "2023-12-01T10:00:00Z"
}
```

## 开发规范

### 组件命名
- 组件文件使用 PascalCase: `UserManagement.vue`
- 组件名称使用 PascalCase: `UserManagement`

### 路由命名
- 路由名称使用 PascalCase: `UserManagement`
- 路由路径使用 kebab-case: `/user-management`

### API文件组织
- 按功能模块组织API文件
- 统一使用 `xxxAPI` 命名导出
- 提供默认导出供直接调用

## 常见问题

### 1. 登录后页面空白
检查用户类型是否为管理员 (`userType === 2`)

### 2. 权限验证失败
确认后端已正确配置管理员权限接口

### 3. 接口请求失败
检查 `VITE_API_BASE_URL` 配置是否正确

## 版本更新

### v1.0.0 (当前版本)
- ✅ 基础项目架构搭建
- ✅ 用户认证系统
- ✅ 权限管理系统
- ✅ 基础UI框架集成

### 计划功能
- 📋 用户管理页面
- 👨‍⚕️ 医生管理页面
- 💬 咨询管理页面
- 📊 数据统计页面
- ⚙️ 系统设置页面

## 许可证

MIT License

## 联系方式

如有问题请联系开发团队。 