<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.tlbglxt.mapper.RoleMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="org.example.tlbglxt.entity.Role">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="role_code" property="roleCode" jdbcType="VARCHAR"/>
        <result column="role_desc" property="roleDesc" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="sort_order" property="sortOrder" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="BIGINT"/>
        <result column="update_by" property="updateBy" jdbcType="BIGINT"/>
        <result column="is_deleted" property="isDeleted" jdbcType="TINYINT"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, role_name, role_code, role_desc, status, sort_order, 
        create_time, update_time, create_by, update_by, is_deleted
    </sql>

    <!-- 根据ID查询角色 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_role
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 根据角色编码查询角色 -->
    <select id="selectByRoleCode" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_role
        WHERE role_code = #{roleCode} AND is_deleted = 0
    </select>

    <!-- 查询所有启用的角色 -->
    <select id="selectAllActive" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_role
        WHERE status = 1 AND is_deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT r.id, r.role_name, r.role_code, r.role_desc, r.status, r.sort_order, 
               r.create_time, r.update_time, r.create_by, r.update_by, r.is_deleted
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.status = 1 AND r.is_deleted = 0
        ORDER BY r.sort_order ASC, r.id ASC
    </select>

    <!-- 插入角色 -->
    <insert id="insert" parameterType="org.example.tlbglxt.entity.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (
            role_name, role_code, role_desc, status, sort_order,
            create_time, update_time, create_by, update_by, is_deleted
        ) VALUES (
            #{roleName}, #{roleCode}, #{roleDesc}, #{status}, #{sortOrder},
            NOW(), NOW(), #{createBy}, #{updateBy}, 0
        )
    </insert>

    <!-- 更新角色 -->
    <update id="updateById" parameterType="org.example.tlbglxt.entity.Role">
        UPDATE sys_role
        <set>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="roleDesc != null">role_desc = #{roleDesc},</if>
            <if test="status != null">status = #{status},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 删除角色（逻辑删除） -->
    <update id="deleteById">
        UPDATE sys_role 
        SET is_deleted = 1, update_time = NOW(), update_by = #{updateBy}
        WHERE id = #{id}
    </update>

    <!-- 检查角色编码是否存在 -->
    <select id="existsByRoleCode" resultType="java.lang.Boolean">
        SELECT COUNT(1) > 0
        FROM sys_role
        WHERE role_code = #{roleCode} AND is_deleted = 0
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

</mapper> 